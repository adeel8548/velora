# Ecommerce Frontend

Quick start

1. Install dependencies:

   ```bash
   cd ecommerce-frontend
   npm install
   npm start
   ```

2. Set `REACT_APP_API_URL` in environment or use default `http://localhost:5000/api`.

This frontend is a lightweight scaffold with pages and API helpers. Extend components and style with Tailwind or Bootstrap.
